function kuchor(val) {
    console.log(val * 2)
    var m = ['hi', 'go', 'good']


    if (5 > 4) {
        console.log('nice')
    }
    while (val > 20) {
        console.log('bahut badhiya')
        val -= 5
    }

    for (var i = 1; i <= 10; i++) {
        console.log(i * val)
    }

    var myString = ['hii', 'go', 'good']
    for (var t in m) {
        console.log(t)
    }

    var names = { name: 'someone', type: 'pata nahi' }
    for (v in names) {
        console.log(v)
    }
    document.write("oh this came from js")








}